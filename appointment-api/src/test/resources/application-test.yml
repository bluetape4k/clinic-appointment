spring:
    application:
        name: appointment-api-test
    datasource:
        url: jdbc:h2:mem:appointment-test;DB_CLOSE_DELAY=-1
        driver-class-name: org.h2.Driver
    flyway:
        enabled: false

# Redis 없는 test profile에서는 leader 보호 없는 reminder runner를 만들지 않습니다.
bluetape4k:
    leader:
        scheduling:
            enabled: false
